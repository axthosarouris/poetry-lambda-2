version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.10
  build:
    on-failure: ABORT
    commands:
      - sam build -t template.yaml
      - sam deploy

artifacts:
  files:
    - template.yaml
